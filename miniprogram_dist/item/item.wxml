<image 
  src="{{img.path}}"
  catch:tap="onTap"
  class="img"
  mode="aspectFill">
    <icon 
      size="20"
      type="clear"
      catch:tap="onTapDel"
      class="close"
      hidden="{{movable}}"
      />
</image>